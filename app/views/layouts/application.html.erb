<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  
  <ul class="nav justify-content-end">
  <% if user_signed_in? %>
    <li class="nav-item">
      <%= button_to "Sign out", destroy_user_session_path, method: :get , class:"btn btn-outline-danger btn-sm"%> 
    </li>
  <%else%>
  <button type="button" class="btn btn-outline-success btn-sm">
    <li class="nav-item">
      <a class="nav-link active" aria-current="page" href="/users/sign_in">Login</a>
    </li>
  </button>&nbsp;
  <button type="button" class="btn btn-outline-info btn-sm">
    <li class="nav-item">
      <a class="nav-link active" aria-current="page" href="/users/sign_up">Sign Up</a>
    </li>
  </button>
  <% end %>
  </ul>
  <% if notice %>
  <div class="alert alert-warning alert-dismissible fade show" role="alert">
    <strong><%= notice %></strong>
  </div>
  <%end%>  
  <% if alert %>
  <div class="alert alert-warning alert-dismissible fade show" role="alert">
    <strong><%= alert %></strong>
  </div>
  <%end%> 
    <%= yield %>
  </body>
</html>
